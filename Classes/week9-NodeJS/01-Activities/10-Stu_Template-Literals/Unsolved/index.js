// music should be an object with title, artist, and album properties
const music = {
  Title: "Yellow Submarine",
  Artist: "Beatles",
  Album: "One of their albums"
 
};

// Write code between the backticks tags to output the data from the music object above.
const songSnippet = 
`The Title is ${music.Title}.
The Artist is ${music.Artist}.
The Album is ${music.Album}.`;

console.log(songSnippet);

